// Home redirects straight to the Configuration Builder project dashboard.
// The full project-list UI lives at /projects if needed.
import { redirect } from 'next/navigation'

export default function Home() {
  redirect('/project/fjbpvnumsh77ah1')
}
